{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"azure-functions-knowledge-python","text":"<p>Knowledge retrieval (RAG) decorators for Azure Functions Python v2.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Decorator-based API \u2014 Seamless integration with Azure Functions Python v2 programming model</li> <li>Provider abstraction \u2014 Pluggable knowledge providers via protocol-based interface</li> <li>Notion support \u2014 Built-in Notion provider for searching and retrieving pages</li> <li>Async support \u2014 Automatic async offloading for non-blocking execution</li> <li>Environment variable resolution \u2014 <code>%VAR%</code> placeholder substitution for secure credential handling</li> </ul>"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li>Installation</li> <li>Getting Started</li> </ul>"},{"location":"agent-playbook/","title":"Agent Playbook","text":""},{"location":"agent-playbook/#source-of-truth","title":"Source Of Truth","text":"<ul> <li><code>AGENTS.md</code> for repository-wide engineering and agent rules.</li> <li><code>README.md</code> for installation, quick start, and CLI examples.</li> <li><code>CONTRIBUTING.md</code> for branch, commit, and release workflow.</li> <li><code>pyproject.toml</code> and <code>Makefile</code> for supported commands.</li> </ul>"},{"location":"agent-playbook/#repository-map","title":"Repository Map","text":"<ul> <li><code>src/azure_functions_knowledge/</code> package code.</li> <li><code>tests/</code> RAG decorator and provider coverage.</li> <li><code>examples/</code> example Azure Functions apps.</li> <li><code>docs/</code> documentation site content.</li> </ul>"},{"location":"agent-playbook/#change-workflow","title":"Change Workflow","text":"<ol> <li>Confirm whether the change affects knowledge API, provider behavior, decorator interface, or docs only.</li> <li>Update examples when they are used as public contract material.</li> <li>Keep tests and docs in lockstep for public behavior changes.</li> <li>Do not broaden supported Python versions or dependency ranges casually.</li> </ol>"},{"location":"agent-playbook/#validation","title":"Validation","text":"<ul> <li><code>make test</code></li> <li><code>make lint</code></li> <li><code>make typecheck</code></li> <li><code>make security</code></li> <li><code>make build</code></li> </ul>"},{"location":"api/","title":"API Reference","text":"<p>The complete public surface of <code>azure-functions-knowledge-python</code>. Every symbol listed here is exported from the top-level <code>azure_functions_knowledge</code> package (and enforced by <code>tests/test_public_api.py</code>), except where noted as internal.</p> <pre><code>from azure_functions_knowledge import (\n    Document,\n    KnowledgeBindings,\n    KnowledgeProvider,\n    create_provider,\n    get_registered_providers,\n    register_provider,\n    AuthError,\n    ConfigurationError,\n    KnowledgeError,\n    ProviderError,\n    __version__,\n)\n</code></pre>"},{"location":"api/#knowledgebindings","title":"<code>KnowledgeBindings</code>","text":"<p>Azure Functions-style decorator API for knowledge retrieval. Instantiate once per app and reuse for every handler:</p> <pre><code>kb = KnowledgeBindings()\n</code></pre>"},{"location":"api/#knowledgebindingsinput","title":"<code>KnowledgeBindings.input</code>","text":"<pre><code>def input(\n    self,\n    arg_name: str,\n    *,\n    provider: str,\n    query: str | Callable[..., str],\n    top: int = 5,\n    connection: str | Mapping[str, str],\n    **kwargs: Any,\n) -&gt; Callable[[Callable[..., Any]], Callable[..., Any]]\n</code></pre> <p>Searches a provider and injects the resulting <code>list[Document]</code> into the handler parameter named <code>arg_name</code>.</p> Parameter Description <code>arg_name</code> Handler parameter that receives <code>list[Document]</code>. Must exist in the handler signature and must not be a reserved name. <code>provider</code> Registered provider name (e.g. <code>\"notion\"</code>). Resolved via <code>create_provider</code>. <code>query</code> Either a static <code>str</code> or a callable. A callable receives handler parameters by name (e.g. <code>lambda req: req.params.get(\"q\", \"\")</code>) and must not use <code>*args</code>/<code>**kwargs</code>. Every parameter the callable references must exist on the handler. <code>top</code> Maximum results to request. Must be <code>&gt;= 1</code> (otherwise <code>ConfigurationError</code>). <code>connection</code> Connection string or mapping. See Configuration. <code>**kwargs</code> Forwarded to the provider constructor (e.g. <code>include_content=False</code>). <p>Behavior:</p> <ul> <li>The provider is created per invocation and closed in a <code>finally</code> block.</li> <li>Async handlers are detected automatically; the blocking search runs in a   worker thread via <code>asyncio.to_thread()</code>.</li> <li>The injected parameter is stripped from the host-facing <code>__signature__</code> so the   Azure Functions host does not attempt to bind it.</li> </ul>"},{"location":"api/#knowledgebindingsinject_client","title":"<code>KnowledgeBindings.inject_client</code>","text":"<pre><code>def inject_client(\n    self,\n    arg_name: str,\n    *,\n    provider: str,\n    connection: str | Mapping[str, str],\n    **kwargs: Any,\n) -&gt; Callable[[Callable[..., Any]], Callable[..., Any]]\n</code></pre> <p>Injects a live provider instance into <code>arg_name</code> for imperative control. Use it when you need to call <code>search()</code> / <code>get_document()</code> yourself, or issue multiple calls per invocation.</p> <ul> <li>For sync handlers the raw provider is injected.</li> <li>For async handlers an <code>_AsyncProviderProxy</code> is injected. It exposes   awaitable <code>search(...)</code> / <code>get_document(...)</code> methods that offload to a worker   thread, plus a synchronous <code>close()</code>.</li> <li>The provider is always closed in a <code>finally</code> block after the handler returns.</li> </ul>"},{"location":"api/#composition-rules","title":"Composition rules","text":"<ul> <li>Azure decorators go outermost; knowledge decorators sit closest to the   function.</li> <li><code>input</code> and <code>inject_client</code> are mutually exclusive on one handler.</li> <li>No knowledge decorator may be applied twice to the same handler.</li> </ul> <p>Violating any rule raises <code>ConfigurationError</code> at decoration time.</p>"},{"location":"api/#reserved-parameter-names","title":"Reserved parameter names","text":"<p><code>arg_name</code> may not be one of the Azure Functions reserved names: <code>context</code>, <code>input</code>, <code>msg</code>, <code>output</code>, <code>req</code>, <code>timer</code>. Using one raises <code>ConfigurationError</code>.</p>"},{"location":"api/#document","title":"<code>Document</code>","text":"<pre><code>@dataclass(kw_only=True)\nclass Document:\n    document_id: str\n    content: str\n    title: str\n    url: str\n    source: str\n    metadata: dict[str, Any] = field(default_factory=dict)\n    score: float | None = None\n</code></pre> <p>A retrieved knowledge document. <code>kw_only=True</code>, so all fields must be passed by keyword. <code>metadata</code> carries provider-specific data (for the Notion provider, raw <code>properties</code> and, for <code>get_document</code>, fetched <code>blocks</code>). <code>score</code> is reserved for relevance ranking and is currently unset by the built-in provider.</p>"},{"location":"api/#provider-registry","title":"Provider registry","text":""},{"location":"api/#knowledgeprovider","title":"<code>KnowledgeProvider</code>","text":"<pre><code>@runtime_checkable\nclass KnowledgeProvider(Protocol):\n    def __init__(\n        self, *, connection: str | Mapping[str, str], **kwargs: Any\n    ) -&gt; None: ...\n    def search(self, query: str, *, top: int = 5) -&gt; list[Document]: ...\n    def get_document(self, document_id: str) -&gt; Document: ...\n    def close(self) -&gt; None: ...\n</code></pre> <p>The structural protocol every provider must satisfy. Because it is <code>runtime_checkable</code>, <code>isinstance(obj, KnowledgeProvider)</code> performs a method-name check. Note that <code>isinstance</code> checks skip dunder methods, so the <code>__init__</code> contract is enforced only by static typing: custom providers must accept a keyword-only <code>connection</code> argument (a string or mapping) plus arbitrary provider-specific <code>**kwargs</code>, matching how <code>create_provider</code> instantiates them. See Usage \u2192 Custom providers.</p>"},{"location":"api/#register_provider","title":"<code>register_provider</code>","text":"<pre><code>def register_provider(name: str, provider_cls: type[KnowledgeProvider]) -&gt; None\n</code></pre> <p>Registers <code>provider_cls</code> under <code>name</code> in the process-wide registry. Re-using an existing name overwrites the previous entry.</p>"},{"location":"api/#create_provider","title":"<code>create_provider</code>","text":"<pre><code>def create_provider(\n    name: str,\n    *,\n    connection: str | Mapping[str, str],\n    **kwargs: Any,\n) -&gt; KnowledgeProvider\n</code></pre> <p>Instantiates a registered provider. Raises <code>ConfigurationError</code> with the message <code>Unknown provider '&lt;name&gt;'. Available: [...]</code> when <code>name</code> is not registered. <code>connection</code> and <code>**kwargs</code> are forwarded to the provider constructor.</p>"},{"location":"api/#get_registered_providers","title":"<code>get_registered_providers</code>","text":"<pre><code>def get_registered_providers() -&gt; list[str]\n</code></pre> <p>Returns the sorted list of registered provider names. <code>\"notion\"</code> is always present because the Notion provider registers itself unconditionally on import.</p>"},{"location":"api/#exceptions","title":"Exceptions","text":"<p>All exceptions derive from <code>KnowledgeError</code>, so a single <code>except KnowledgeError</code> catches every failure this package raises.</p> Exception Raised when <code>KnowledgeError</code> Base class for all errors below. <code>ConfigurationError</code> Invalid decorator usage (bad <code>arg_name</code>, <code>top &lt; 1</code>, illegal composition), unknown provider, or an unset <code>%VAR%</code> in a connection string. <code>ProviderError</code> A provider failed at runtime \u2014 e.g. <code>notion-client</code> is not installed, or the Notion API returned an error. <code>AuthError</code> Authentication/credential problems \u2014 e.g. a connection mapping missing <code>token</code>/<code>api_key</code>, or Notion client initialization failure."},{"location":"api/#resolve_connection-internal-helper","title":"<code>resolve_connection</code> (internal helper)","text":"<pre><code>from azure_functions_knowledge.auth import resolve_connection\n\ndef resolve_connection(value: str) -&gt; str\n</code></pre> <p>Note</p> <p><code>resolve_connection</code> is not exported from the top-level package \u2014 import it from <code>azure_functions_knowledge.auth</code> if you need it directly. It is documented here because providers rely on it and custom providers may reuse it.</p> <p>Substitutes <code>%VAR%</code> placeholders in <code>value</code> with the matching environment variables. Raises <code>ConfigurationError</code> when a referenced variable is unset. See Configuration.</p>"},{"location":"api/#__version__","title":"<code>__version__</code>","text":"<p>The installed package version string, kept in sync with the distribution metadata (<code>importlib.metadata.version(\"azure-functions-knowledge-python\")</code>).</p>"},{"location":"architecture/","title":"Architecture","text":"<p>This page explains what happens between a decorated Azure Functions handler and a knowledge provider, and provides the canonical architecture diagram for the package. (<code>DESIGN.md</code> links here.)</p>"},{"location":"architecture/#components","title":"Components","text":"Component File Responsibility <code>KnowledgeBindings</code> <code>src/azure_functions_knowledge/decorator.py</code> Public decorator API (<code>input</code>, <code>inject_client</code>). Validates arg names, composition, and query callables; builds the host-facing signature. Provider registry <code>src/azure_functions_knowledge/providers/base.py</code> <code>register_provider</code> / <code>create_provider</code> / <code>get_registered_providers</code> over a process-wide name \u2192 class map. <code>KnowledgeProvider</code> <code>src/azure_functions_knowledge/providers/base.py</code> Structural <code>Protocol</code> (<code>search</code>, <code>get_document</code>, <code>close</code>). <code>NotionProvider</code> <code>src/azure_functions_knowledge/providers/notion.py</code> Built-in provider backed by the Notion API. <code>resolve_connection</code> <code>src/azure_functions_knowledge/auth.py</code> <code>%VAR%</code> environment-variable substitution for connection strings. <code>_AsyncProviderProxy</code> <code>src/azure_functions_knowledge/decorator.py</code> Wraps a sync provider so async handlers can <code>await</code> its calls. <code>Document</code> <code>src/azure_functions_knowledge/types.py</code> Dataclass returned to handlers. Errors <code>src/azure_functions_knowledge/errors.py</code> <code>KnowledgeError</code> hierarchy."},{"location":"architecture/#request-flow","title":"Request flow","text":"<p>At decoration time the binding validates configuration and replaces the handler with a wrapper. At invocation time the wrapper resolves the query, creates a provider, runs the operation, injects the result, and always closes the provider. The diagram below includes the async offload path (<code>_AsyncProviderProxy</code> / <code>asyncio.to_thread</code>), the <code>resolve_connection</code> step, and the error paths.</p> flowchart TD     HOST[\"Azure Functions host\"] --&gt;|invokes wrapper| WRAP[\"@kb.input / @kb.inject_client wrapper\"]      subgraph Decoration [\"Decoration time\"]         DEC1[\"@kb.input(...)\"] --&gt; VAL[\"arg-name + composition\\n+ query-callable validation\"]         DEC2[\"@kb.inject_client(...)\"] --&gt; VAL         VAL --&gt; SIG[\"build host __signature__\\n(hide injected arg)\"]     end      WRAP --&gt; QRES[\"resolve query\\n(static or callable)\"]     QRES --&gt; FACTORY[\"create_provider(name, connection, **kwargs)\"]     FACTORY --&gt;|unknown name| ERRC[\"ConfigurationError\"]     FACTORY --&gt; REG[\"provider registry\\nregister_provider()\"]     REG --&gt; PROV[\"Provider.__init__\\n(Notion / custom)\"]      PROV --&gt; RESOLVE[\"resolve_connection('%VAR%')\"]     RESOLVE --&gt;|unset var| ERRC     PROV --&gt;|missing extra| ERRP[\"ProviderError\"]     PROV --&gt;|bad token / init fail| ERRA[\"AuthError\"]      PROV --&gt; ASYNCQ{\"async handler?\"}     ASYNCQ --&gt;|yes| PROXY[\"_AsyncProviderProxy\\nasyncio.to_thread(...)\"]     ASYNCQ --&gt;|no| DIRECT[\"direct call\"]     PROXY --&gt; OP[\"search(query, top)\\n/ get_document(id)\"]     DIRECT --&gt; OP      OP --&gt; DOCS[\"list[Document] / Document\"]     DOCS --&gt; INJECT[\"inject into handler arg\"]     INJECT --&gt; CLOSE[\"provider.close() (finally)\"]     CLOSE --&gt; HANDLER[\"handler body runs\"] <p>The <code>input</code> decorator injects the results (<code>list[Document]</code>); <code>inject_client</code> injects the provider (or an async proxy) so the handler drives the calls itself.</p>"},{"location":"architecture/#async-offloading","title":"Async offloading","text":"<p>Providers are synchronous. When a handler is <code>async</code>, the binding keeps the event loop responsive by offloading blocking I/O to a worker thread:</p> <ul> <li><code>input</code> wraps the whole search (<code>create_provider</code> + <code>search</code> + <code>close</code>) in   <code>asyncio.to_thread(...)</code>.</li> <li><code>inject_client</code> injects an <code>_AsyncProviderProxy</code> whose <code>search</code> /   <code>get_document</code> methods each call <code>asyncio.to_thread(...)</code> on the underlying   provider, so the handler can <code>await</code> them.</li> </ul>"},{"location":"architecture/#provider-lifecycle","title":"Provider lifecycle","text":"<p>A provider is constructed per invocation and closed in a <code>finally</code> block \u2014 there is no connection pooling or caching across invocations. Keep constructors cheap, or cache expensive resources at module scope inside your provider implementation.</p>"},{"location":"architecture/#error-paths","title":"Error paths","text":"Situation Exception Unknown provider name <code>ConfigurationError</code> Unset <code>%VAR%</code> in connection string <code>ConfigurationError</code> Invalid decorator usage / composition <code>ConfigurationError</code> Optional extra missing (e.g. <code>notion-client</code>) <code>ProviderError</code> Provider runtime/API failure <code>ProviderError</code> Bad credentials / mapping without token <code>AuthError</code>"},{"location":"architecture/#notion-provider-behavior","title":"Notion provider behavior","text":"<p><code>NotionProvider</code> (<code>providers/notion.py</code>) registers unconditionally so <code>create_provider(\"notion\")</code> reaches an actionable <code>ProviderError</code> when the <code>[notion]</code> extra is missing. Content extraction walks child blocks depth-first with pagination and recursion, bounded by <code>max_depth</code> / <code>max_blocks</code> and guarded against cycles. Provider options are documented in the package docs.</p>"},{"location":"configuration/","title":"Configuration","text":"<p>Every decorator takes a <code>connection</code> argument that tells the provider how to authenticate. Connections support <code>%VAR%</code> environment-variable substitution so you never have to hard-code secrets.</p>"},{"location":"configuration/#connection-forms","title":"Connection forms","text":"<p><code>connection</code> accepts either a string or a mapping:</p> <pre><code>connection=\"%NOTION_TOKEN%\"          # single env var\nconnection=\"Bearer %API_KEY%\"        # partial substitution inside a larger string\nconnection={\"token\": \"%MY_TOKEN%\"}   # mapping with substitution\nconnection={\"api_key\": \"%MY_TOKEN%\"} # 'api_key' is accepted as an alias for 'token'\n</code></pre>"},{"location":"configuration/#var-substitution","title":"<code>%VAR%</code> substitution","text":"<p>Placeholders match the pattern <code>%NAME%</code>, where <code>NAME</code> starts with a letter or underscore and continues with letters, digits, or underscores. Each placeholder is replaced with the corresponding environment variable. Multiple placeholders in one string are all resolved.</p> <pre><code>import os\nos.environ[\"NOTION_TOKEN\"] = \"secret_abc123\"\n\n# \"%NOTION_TOKEN%\"        -&gt; \"secret_abc123\"\n# \"Bearer %NOTION_TOKEN%\" -&gt; \"Bearer secret_abc123\"\n</code></pre> <p>Substitution is performed by <code>azure_functions_knowledge.auth.resolve_connection</code>.</p>"},{"location":"configuration/#error-semantics","title":"Error semantics","text":"<p>If a referenced variable is not set, <code>resolve_connection</code> raises <code>ConfigurationError</code>:</p> <pre><code>Environment variable 'NOTION_TOKEN' referenced in connection string is not set\n</code></pre> <p>Because provider construction happens per invocation, this surfaces at request time \u2014 set the variable in your Function App's application settings (or your local <code>local.settings.json</code>) before deploying.</p>"},{"location":"configuration/#notion-token-mapping","title":"Notion token mapping","text":"<p><code>NotionProvider</code> accepts a connection in two shapes:</p> <ul> <li>String \u2014 resolved through <code>%VAR%</code> substitution, then used directly as the   Notion integration token.</li> <li>Mapping \u2014 the token is read from the <code>token</code> key, falling back to   <code>api_key</code>. The resolved value is then passed through <code>%VAR%</code> substitution.   If neither key is present, an <code>AuthError</code> is raised:</li> </ul> <pre><code>NotionProvider connection mapping must contain 'token' or 'api_key'\n</code></pre> <p>If the Notion client fails to initialize with the resolved token, an <code>AuthError</code> is raised with the underlying cause attached.</p>"},{"location":"configuration/#passing-provider-options","title":"Passing provider options","text":"<p>Any keyword arguments beyond <code>connection</code> are forwarded to the provider constructor. For Notion, for example:</p> <pre><code>@kb.input(\n    \"docs\",\n    provider=\"notion\",\n    query=\"roadmap\",\n    connection=\"%NOTION_TOKEN%\",\n    include_content=False,   # forwarded to NotionProvider\n    content_max_chars=2000,  # forwarded to NotionProvider\n)\ndef handler(req, docs): ...\n</code></pre> <p>See Providers \u2192 Notion for the full list of options.</p>"},{"location":"faq/","title":"FAQ","text":""},{"location":"faq/#which-python-versions-are-supported","title":"Which Python versions are supported?","text":"<p>Python 3.10 through 3.14 (<code>&gt;=3.10, &lt;3.15</code>).</p>"},{"location":"faq/#do-i-always-need-the-notion-extra","title":"Do I always need the Notion extra?","text":"<p>Only if you use the built-in Notion provider. Custom providers have no such dependency. Install with <code>pip install azure-functions-knowledge[notion]</code> when you need Notion.</p>"},{"location":"faq/#can-i-use-this-with-async-handlers","title":"Can I use this with async handlers?","text":"<p>Yes. Both <code>input</code> and <code>inject_client</code> detect <code>async def</code> handlers and offload blocking provider I/O to a worker thread. With <code>inject_client</code>, the async handler receives a proxy whose methods you <code>await</code>. See Usage \u2192 Async handlers.</p>"},{"location":"faq/#is-the-provider-reused-across-invocations","title":"Is the provider reused across invocations?","text":"<p>No. A provider is created per invocation and closed afterward in a <code>finally</code> block. There is no built-in pooling \u2014 cache expensive resources inside your provider implementation if needed.</p>"},{"location":"faq/#how-do-i-add-my-own-knowledge-source","title":"How do I add my own knowledge source?","text":"<p>Implement the <code>KnowledgeProvider</code> protocol and call <code>register_provider(\"my-name\", MyProvider)</code>. See Usage \u2192 Custom provider registration.</p>"},{"location":"faq/#where-does-the-injected-parameter-come-from","title":"Where does the injected parameter come from?","text":"<p>The decorators hide the injected parameter from the host-facing signature and supply it themselves at invocation time, so the Azure Functions host never tries to bind it.</p>"},{"location":"faq/#what-is-documentscore","title":"What is <code>Document.score</code>?","text":"<p>A reserved field for relevance ranking. It is part of the public dataclass but is not populated by the built-in Notion provider today.</p>"},{"location":"faq/#how-do-i-keep-secrets-out-of-my-code","title":"How do I keep secrets out of my code?","text":"<p>Use <code>%VAR%</code> placeholders in <code>connection</code> and store the real values in your Function App application settings or <code>local.settings.json</code>. See Configuration.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#setup","title":"Setup","text":"<ol> <li>Install the package with the Notion provider:</li> </ol> <pre><code>pip install azure-functions-knowledge[notion]\n</code></pre> <ol> <li>Set up your Notion API token as an environment variable:</li> </ol> <pre><code>export NOTION_TOKEN=\"your-notion-integration-token\"\n</code></pre>"},{"location":"getting-started/#data-injection-with-input","title":"Data Injection with <code>input</code>","text":"<p>The <code>input</code> decorator searches a knowledge provider and injects results directly into your handler:</p> <pre><code>import azure.functions as func\nfrom azure_functions_knowledge import Document, KnowledgeBindings\n\napp = func.FunctionApp()\nkb = KnowledgeBindings()\n\n@app.route(route=\"search\", methods=[\"GET\"])\n@kb.input(\n    \"docs\",\n    provider=\"notion\",\n    query=lambda req: req.params.get(\"q\", \"\"),\n    top=5,\n    connection=\"%NOTION_TOKEN%\",\n)\ndef search(req: func.HttpRequest, docs: list[Document]) -&gt; func.HttpResponse:\n    import json\n    results = [{\"title\": d.title, \"url\": d.url} for d in docs]\n    return func.HttpResponse(json.dumps(results), mimetype=\"application/json\")\n</code></pre>"},{"location":"getting-started/#static-vs-dynamic-queries","title":"Static vs Dynamic Queries","text":"<p>Static query \u2014 Same search every invocation:</p> <pre><code>@kb.input(\"docs\", provider=\"notion\", query=\"project roadmap\", connection=\"%NOTION_TOKEN%\")\ndef handler(timer, docs: list[Document]) -&gt; None:\n    ...\n</code></pre> <p>Dynamic query \u2014 Derived from handler parameters:</p> <pre><code>@kb.input(\n    \"docs\",\n    provider=\"notion\",\n    query=lambda req: req.params.get(\"q\", \"\"),\n    connection=\"%NOTION_TOKEN%\",\n)\ndef handler(req: func.HttpRequest, docs: list[Document]) -&gt; func.HttpResponse:\n    ...\n</code></pre>"},{"location":"getting-started/#client-injection-with-inject_client","title":"Client Injection with <code>inject_client</code>","text":"<p>For imperative control, use <code>inject_client</code> to receive a provider instance:</p> <pre><code>@app.route(route=\"page/{page_id}\", methods=[\"GET\"])\n@kb.inject_client(\"client\", provider=\"notion\", connection=\"%NOTION_TOKEN%\")\ndef get_page(req: func.HttpRequest, client) -&gt; func.HttpResponse:\n    import json\n    page_id = req.route_params.get(\"page_id\", \"\")\n    doc = client.get_document(page_id)\n    return func.HttpResponse(\n        json.dumps({\"title\": doc.title, \"content\": doc.content}),\n        mimetype=\"application/json\",\n    )\n</code></pre>"},{"location":"getting-started/#decorator-composition-rules","title":"Decorator Composition Rules","text":"<ul> <li>Azure decorators go outermost, knowledge decorators closest to the function</li> <li><code>input</code> and <code>inject_client</code> are mutually exclusive on the same handler</li> <li>No decorator can be applied twice to the same handler</li> </ul>"},{"location":"getting-started/#connection-strings","title":"Connection Strings","text":"<p>Connection parameters support <code>%VAR%</code> environment variable substitution:</p> <pre><code>connection=\"%NOTION_TOKEN%\"          # Single env var\nconnection=\"Bearer %API_KEY%\"        # Partial substitution\nconnection={\"token\": \"%MY_TOKEN%\"}   # Mapping with substitution\n</code></pre>"},{"location":"getting-started/#async-support","title":"Async Support","text":"<p>Both <code>input</code> and <code>inject_client</code> automatically detect async handlers and offload blocking I/O via <code>asyncio.to_thread()</code>:</p> <pre><code>@app.route(route=\"search\", methods=[\"GET\"])\n@kb.input(\"docs\", provider=\"notion\", query=\"roadmap\", connection=\"%NOTION_TOKEN%\")\nasync def search(req: func.HttpRequest, docs: list[Document]) -&gt; func.HttpResponse:\n    ...\n</code></pre>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#basic-installation","title":"Basic Installation","text":"<pre><code>pip install azure-functions-knowledge\n</code></pre>"},{"location":"installation/#with-notion-provider","title":"With Notion Provider","text":"<pre><code>pip install azure-functions-knowledge[notion]\n</code></pre>"},{"location":"installation/#with-all-providers","title":"With All Providers","text":"<pre><code>pip install azure-functions-knowledge[all]\n</code></pre>"},{"location":"installation/#development-installation","title":"Development Installation","text":"<pre><code>git clone https://github.com/yeongseon/azure-functions-knowledge-python.git\ncd azure-functions-knowledge-python\nmake install\n</code></pre>"},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.11+</li> <li>Azure Functions Python v2 programming model</li> </ul>"},{"location":"release_process/","title":"Release Process","text":"<p>This document outlines the steps to release a new version of azure-functions-knowledge-python to PyPI and update the changelog using the existing Makefile and Hatch-based workflows.</p>"},{"location":"release_process/#step-1-bump-version-and-generate-changelog","title":"Step 1: Bump Version and Generate Changelog","text":"<p>Use Makefile targets to bump the version and update the changelog:</p> <pre><code>make release-patch     # Patch release (e.g., v0.10.0 -&gt; v0.10.1)\nmake release-minor     # Minor release (e.g., v0.10.1 -&gt; v0.11.0)\nmake release-major     # Major release (e.g., v0.11.0 -&gt; v1.0.0)\n</code></pre> <p>Each command will:</p> <ol> <li>Update the version in <code>src/azure_functions_knowledge/__init__.py</code></li> <li>Generate or update <code>CHANGELOG.md</code> via <code>git-cliff</code></li> <li>Commit the version bump and changelog</li> <li>Create a Git tag (e.g., <code>v0.11.0</code>) and push to <code>main</code></li> </ol> <p>Make sure your <code>main</code> branch is up-to-date before running these commands.</p>"},{"location":"release_process/#changelog-generation","title":"Changelog Generation","text":"<p>The changelog is generated automatically by git-cliff from conventional commit messages.</p>"},{"location":"release_process/#configuration","title":"Configuration","text":"<ul> <li><code>cliff.toml</code> - defines commit grouping, categories, and output format</li> <li><code>Makefile</code> - <code>make changelog</code> runs <code>git-cliff -o CHANGELOG.md</code></li> </ul>"},{"location":"release_process/#commit-message-convention","title":"Commit Message Convention","text":"<p>Follow Conventional Commits for proper changelog grouping:</p> Prefix Changelog Category <code>feat:</code> Features <code>fix:</code> Bug Fixes <code>docs:</code> Documentation <code>refactor:</code> Refactor <code>style:</code> Styling <code>test:</code> Testing <code>perf:</code> Performance <code>ci:</code> / <code>chore:</code> Miscellaneous Tasks <code>build:</code> Other <p>Use scopes for more context: <code>fix(openapi): preserve explicit 200 response</code></p>"},{"location":"release_process/#manual-changelog-regeneration","title":"Manual Changelog Regeneration","text":"<pre><code>make changelog           # Regenerate CHANGELOG.md from all tags\nmake commit-changelog    # Stage and commit the updated changelog\n</code></pre>"},{"location":"release_process/#step-2-build-and-test-the-package","title":"Step 2: Build and Test the Package","text":"<pre><code>make build\n</code></pre> <p>To test the local build:</p> <pre><code>pip install dist/azure_functions_knowledge-&lt;version&gt;-py3-none-any.whl\n</code></pre>"},{"location":"release_process/#step-3-publish-to-pypi-github-actions-primary","title":"Step 3: Publish to PyPI (GitHub Actions, primary)","text":"<p>The <code>.github/workflows/publish-pypi.yml</code> workflow builds and uploads the distribution to PyPI on every <code>v*</code> tag push (or on demand via <code>workflow_dispatch</code>). It uses PyPI trusted publishing over OIDC, so no PyPI API token is stored as a secret.</p> <p>Pushing a release tag (produced by the <code>make release-*</code> targets in Step 1) is therefore all that is normally required to publish.</p>"},{"location":"release_process/#trusted-publisher-configuration","title":"Trusted publisher configuration","text":"<p>PyPI authorizes the upload by matching the OIDC claims presented by GitHub Actions against a trusted publisher registered on the PyPI project. The PyPI publisher config must use these exact values:</p> Field on PyPI publisher form Value PyPI project <code>azure-functions-knowledge</code> Owner <code>yeongseon</code> Repository name <code>azure-functions-knowledge-python</code> Workflow filename <code>publish-pypi.yml</code> Environment name <code>pypi</code> <p>Notes:</p> <ul> <li>Workflow filename is the file under <code>.github/workflows/</code> (<code>publish-pypi.yml</code>), not the workflow display name (<code>Publish to PyPI</code>). Entering the display name will not match.</li> <li>Repository name is the GitHub repository slug (<code>azure-functions-knowledge-python</code> with the <code>-python</code> suffix from the toolkit-wide rename), not the PyPI project name (<code>azure-functions-knowledge</code>) and not the Python import name (<code>azure_functions_knowledge</code>).</li> <li>Environment name matches <code>environment: pypi</code> declared on the <code>build-and-publish</code> job in the workflow. Renaming that environment requires updating the PyPI publisher record.</li> <li>Enter all values exactly as shown; mismatches in repository name, workflow filename, environment, or project name will cause <code>invalid-publisher</code> at upload time.</li> </ul> <p>To register the publisher on PyPI:</p> <ul> <li>If the PyPI project <code>azure-functions-knowledge</code> already exists, open the project page on PyPI \u2192 Manage \u2192 Publishing \u2192 Add a new publisher and enter the values above.</li> <li>If the PyPI project does not exist yet (first release), register a pending publisher under the maintainer's PyPI account with the same values; the first successful upload will then create the project. A pending publisher does not reserve the project name on PyPI \u2014 anyone else can still register the project before the first publish, so cut the first release promptly.</li> </ul>"},{"location":"release_process/#re-run-a-failed-publish","title":"Re-run a failed publish","text":"<p>After updating the trusted publisher in PyPI, the existing tag can be re-published without cutting a new version:</p> <ul> <li>From the GitHub Actions UI, open the failed <code>Publish to PyPI</code> run and click Re-run failed jobs, or</li> <li>Trigger the workflow via <code>workflow_dispatch</code> with the existing tag as input (for example <code>v0.1.0</code>); the workflow checks out that tag and republishes.</li> </ul> <p>Confirm the run succeeds and the new version appears at <code>https://pypi.org/project/azure-functions-knowledge/</code>.</p>"},{"location":"release_process/#local-fallback-manual","title":"Local fallback (manual)","text":"<p>Only needed if the GitHub Actions publish is unavailable; the OIDC workflow above is the supported path.</p> <pre><code>make publish-pypi\n</code></pre> <ul> <li>Uses <code>hatch publish</code> under the hood</li> <li>Relies on <code>~/.pypirc</code> for authentication (must contain a PyPI token)</li> </ul>"},{"location":"release_process/#step-4-optional-publish-to-testpypi","title":"Step 4: (Optional) Publish to TestPyPI","text":"<pre><code>make publish-test\n</code></pre> <p>To install from TestPyPI:</p> <pre><code>pip install --index-url https://test.pypi.org/simple/ azure-functions-knowledge\n</code></pre>"},{"location":"release_process/#summary-of-makefile-commands","title":"Summary of Makefile Commands","text":"Task Command Version bump + changelog <code>make release-patch</code> / <code>release-minor</code> / <code>release-major</code> Build distributions <code>make build</code> Publish to PyPI <code>make publish-pypi</code> Publish to TestPyPI <code>make publish-test</code> Regenerate changelog only <code>make changelog</code> Show current version <code>make version</code>"},{"location":"release_process/#related","title":"Related","text":"<ul> <li>CHANGELOG.md</li> <li>Contributing</li> <li>PyPI Publishing with Hatch</li> </ul>"},{"location":"testing/","title":"Testing","text":"<p>How to run the test suite and how to test handlers that use knowledge bindings.</p>"},{"location":"testing/#running-the-suite","title":"Running the suite","text":"<pre><code>make install        # set up the dev environment\nmake test           # run pytest with coverage\n</code></pre> <p>Or directly with hatch:</p> <pre><code>hatch run pytest --cov --cov-report=term-missing -q\n</code></pre> <p>The project enforces a 95% coverage floor; changes that drop below it must add tests to compensate.</p>"},{"location":"testing/#full-local-check","title":"Full local check","text":"<p>Mirror CI before opening a PR:</p> <pre><code>make lint        # ruff\nmake typecheck   # mypy\nmake test        # pytest + coverage\nmake build       # build the distribution\n</code></pre>"},{"location":"testing/#testing-your-handlers","title":"Testing your handlers","text":"<p>Because <code>input</code>/<code>inject_client</code> create the provider through the registry, the cleanest way to test a handler is to register a fake provider:</p> <pre><code>from azure_functions_knowledge import Document, register_provider\n\nclass FakeProvider:\n    def __init__(self, *, connection, **kwargs):\n        pass\n\n    def search(self, query: str, *, top: int = 5) -&gt; list[Document]:\n        return [Document(\n            document_id=\"1\", content=\"hello\", title=\"Doc\",\n            url=\"https://example.test/1\", source=\"fake\",\n        )]\n\n    def get_document(self, document_id: str) -&gt; Document:\n        return Document(\n            document_id=document_id, content=\"hello\", title=\"Doc\",\n            url=\"https://example.test/1\", source=\"fake\",\n        )\n\n    def close(self) -&gt; None:\n        pass\n\nregister_provider(\"fake\", FakeProvider)\n</code></pre> <p>Then decorate a handler with <code>provider=\"fake\"</code> and call it directly. No network access or Notion token is required.</p>"},{"location":"testing/#ci-environment","title":"CI environment","text":"<p>The <code>ci-test.yml</code> workflow runs the same lint / typecheck / test steps across the supported Python versions and uploads coverage. No secrets are required to run the suite because the built-in tests do not hit the live Notion API.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>Common problems and how to resolve them.</p>"},{"location":"troubleshooting/#providererror-notion-client-is-required-for-notionprovider","title":"<code>ProviderError: notion-client is required for NotionProvider</code>","text":"<p>The optional Notion dependency is not installed. Install the extra:</p> <pre><code>pip install azure-functions-knowledge[notion]\n</code></pre>"},{"location":"troubleshooting/#configurationerror-environment-variable-referenced-in-connection-string-is-not-set","title":"<code>ConfigurationError: Environment variable '...' referenced in connection string is not set</code>","text":"<p>A <code>%VAR%</code> placeholder in your <code>connection</code> has no matching environment variable. Set it in your Function App application settings (or <code>local.settings.json</code> for local runs) before invoking the function. See Configuration.</p>"},{"location":"troubleshooting/#configurationerror-unknown-provider","title":"<code>ConfigurationError: Unknown provider '...'</code>","text":"<p>The <code>provider</code> name is not registered. Check spelling, confirm the provider was imported/registered before the handler runs, and use <code>get_registered_providers()</code> to list what is available.</p>"},{"location":"troubleshooting/#autherror-notionprovider-connection-mapping-must-contain-token-or-api_key","title":"<code>AuthError: NotionProvider connection mapping must contain 'token' or 'api_key'</code>","text":"<p>When passing a mapping connection, include a <code>token</code> (or <code>api_key</code>) key:</p> <pre><code>connection={\"token\": \"%NOTION_TOKEN%\"}\n</code></pre>"},{"location":"troubleshooting/#configurationerror-arg_name-conflicts-with-azure-functions-reserved-parameter-name","title":"<code>ConfigurationError: ... arg_name='...' conflicts with Azure Functions reserved parameter name</code>","text":"<p>The injected parameter name collides with a host-reserved name (<code>context</code>, <code>input</code>, <code>msg</code>, <code>output</code>, <code>req</code>, <code>timer</code>). Rename the injected parameter \u2014 for example use <code>docs</code> instead of <code>input</code>.</p>"},{"location":"troubleshooting/#configurationerror-cannot-combine-input-and-inject_client","title":"<code>ConfigurationError: Cannot combine 'input' and 'inject_client' ...</code>","text":"<p>The two decorators are mutually exclusive on one handler. Pick one.</p>"},{"location":"troubleshooting/#notion-returns-fewerempty-results-than-expected","title":"Notion returns fewer/empty results than expected","text":"<ul> <li>Ensure your Notion integration has access to the pages/databases you expect   (share them with the integration in Notion).</li> <li>If content is missing, confirm <code>include_content=True</code> (the default).</li> <li>Very large pages may hit <code>max_blocks</code> / <code>max_depth</code> caps \u2014 raise them if you   need deeper extraction, but mind Notion rate limits.</li> </ul>"},{"location":"troubleshooting/#notion-api-rate-limiting","title":"Notion API rate limiting","text":"<p>Reduce request volume by lowering <code>max_blocks</code> / <code>max_depth</code>, or set <code>include_content=False</code> when you only need titles and URLs. See Providers \u2192 Notion.</p>"},{"location":"usage/","title":"Usage","text":"<p>Practical recipes for the two decorators, async handlers, error handling, and custom providers. Runnable versions of these live in the <code>examples/</code> directory.</p>"},{"location":"usage/#static-vs-dynamic-queries","title":"Static vs dynamic queries","text":"<p>A static query runs the same search every invocation:</p> <pre><code>@kb.input(\"docs\", provider=\"notion\", query=\"project roadmap\", connection=\"%NOTION_TOKEN%\")\ndef handler(timer, docs: list[Document]) -&gt; None:\n    for doc in docs:\n        print(doc.title, doc.url)\n</code></pre> <p>A dynamic query derives the search string from handler parameters. The callable receives handler parameters by name and must not use <code>*args</code>/<code>**kwargs</code>:</p> <pre><code>@app.route(route=\"search\", methods=[\"GET\"])\n@kb.input(\n    \"docs\",\n    provider=\"notion\",\n    query=lambda req: req.params.get(\"q\", \"\"),\n    top=5,\n    connection=\"%NOTION_TOKEN%\",\n)\ndef search(req: func.HttpRequest, docs: list[Document]) -&gt; func.HttpResponse:\n    import json\n    results = [{\"title\": d.title, \"url\": d.url} for d in docs]\n    return func.HttpResponse(json.dumps(results), mimetype=\"application/json\")\n</code></pre> <p>Referencing a parameter the handler does not have raises <code>ConfigurationError</code> at decoration time.</p>"},{"location":"usage/#async-handlers","title":"Async handlers","text":"<p>Both decorators detect <code>async def</code> handlers and offload blocking provider I/O to a worker thread, so the event loop stays responsive:</p> <pre><code>@app.route(route=\"search\", methods=[\"GET\"])\n@kb.input(\"docs\", provider=\"notion\", query=\"roadmap\", connection=\"%NOTION_TOKEN%\")\nasync def search(req: func.HttpRequest, docs: list[Document]) -&gt; func.HttpResponse:\n    ...\n</code></pre>"},{"location":"usage/#client-injection","title":"Client injection","text":"<p>Use <code>inject_client</code> when you need imperative control \u2014 multiple calls, <code>get_document</code>, or conditional logic:</p> <pre><code>@app.route(route=\"page/{page_id}\", methods=[\"GET\"])\n@kb.inject_client(\"client\", provider=\"notion\", connection=\"%NOTION_TOKEN%\")\ndef get_page(req: func.HttpRequest, client) -&gt; func.HttpResponse:\n    import json\n    page_id = req.route_params.get(\"page_id\", \"\")\n    doc = client.get_document(page_id)\n    return func.HttpResponse(\n        json.dumps({\"title\": doc.title, \"content\": doc.content}),\n        mimetype=\"application/json\",\n    )\n</code></pre> <p>In an async handler the injected client is an async proxy \u2014 <code>await</code> its methods:</p> <pre><code>@app.route(route=\"page/{page_id}\", methods=[\"GET\"])\n@kb.inject_client(\"client\", provider=\"notion\", connection=\"%NOTION_TOKEN%\")\nasync def get_page(req: func.HttpRequest, client) -&gt; func.HttpResponse:\n    doc = await client.get_document(req.route_params[\"page_id\"])\n    results = await client.search(\"related\", top=10)\n    ...\n</code></pre>"},{"location":"usage/#error-handling","title":"Error handling","text":"<p>All errors derive from <code>KnowledgeError</code>, so you can catch broadly or narrowly:</p> <pre><code>from azure_functions_knowledge import (\n    AuthError,\n    ConfigurationError,\n    ProviderError,\n)\n\n@app.route(route=\"search\", methods=[\"GET\"])\n@kb.inject_client(\"client\", provider=\"notion\", connection=\"%NOTION_TOKEN%\")\ndef search(req: func.HttpRequest, client) -&gt; func.HttpResponse:\n    try:\n        docs = client.search(req.params.get(\"q\", \"\"), top=5)\n    except AuthError:\n        return func.HttpResponse(\"Invalid credentials\", status_code=401)\n    except ConfigurationError:\n        return func.HttpResponse(\"Misconfigured connection\", status_code=500)\n    except ProviderError:\n        return func.HttpResponse(\"Upstream provider error\", status_code=502)\n    ...\n</code></pre> <p>See the API reference for which error each situation raises.</p>"},{"location":"usage/#custom-provider-registration","title":"Custom provider registration","text":"<p>Implement the <code>KnowledgeProvider</code> protocol and register it under a name. Constructors must accept a keyword-only <code>connection</code> argument and may accept extra keyword arguments:</p> <pre><code>from azure_functions_knowledge import Document, register_provider\n\nclass StaticProvider:\n    def __init__(self, *, connection, **kwargs):\n        self._docs = kwargs.get(\"docs\", [])\n\n    def search(self, query: str, *, top: int = 5) -&gt; list[Document]:\n        matches = [d for d in self._docs if query.lower() in d.content.lower()]\n        return matches[:top]\n\n    def get_document(self, document_id: str) -&gt; Document:\n        return next(d for d in self._docs if d.document_id == document_id)\n\n    def close(self) -&gt; None:\n        pass\n\nregister_provider(\"static\", StaticProvider)\n\n# Now usable by name:\n@kb.input(\"docs\", provider=\"static\", query=\"hello\", connection=\"unused\")\ndef handler(timer, docs: list[Document]) -&gt; None:\n    ...\n</code></pre> <p>Any keyword arguments you pass to <code>input</code> / <code>inject_client</code> beyond the reserved ones are forwarded to your provider constructor.</p>"},{"location":"providers/notion/","title":"Notion Provider","text":"<p><code>NotionProvider</code> is the built-in <code>KnowledgeProvider</code> backed by the Notion API. It is registered under the name <code>\"notion\"</code>.</p>"},{"location":"providers/notion/#installation","title":"Installation","text":"<p>The provider depends on the optional <code>notion-client</code> package, installed via the <code>notion</code> extra:</p> <pre><code>pip install azure-functions-knowledge[notion]\n</code></pre> <p>The provider is registered unconditionally, even when <code>notion-client</code> is not installed. Attempting to create it without the extra raises a <code>ProviderError</code> with an actionable install hint rather than a generic \"unknown provider\" error.</p>"},{"location":"providers/notion/#authentication","title":"Authentication","text":"<p>Pass a Notion integration token via <code>connection</code>. See Configuration for the accepted forms:</p> <pre><code>connection=\"%NOTION_TOKEN%\"           # string, resolved from env\nconnection={\"token\": \"%NOTION_TOKEN%\"} # mapping ('api_key' also accepted)\n</code></pre>"},{"location":"providers/notion/#options","title":"Options","text":"<p>All options are keyword arguments forwarded through the decorator:</p> Option Default Description <code>include_content</code> <code>True</code> When <code>True</code>, <code>search</code> fetches and renders page content into <code>Document.content</code>. Set <code>False</code> to return metadata only (faster, fewer API calls). <code>content_max_chars</code> <code>None</code> Truncates rendered content to this many characters. <code>None</code> means no limit. <code>max_depth</code> <code>8</code> Maximum block-tree recursion depth when extracting content. <code>max_blocks</code> <code>1000</code> Maximum number of blocks fetched per page. <p>Example:</p> <pre><code>@app.route(route=\"search\", methods=[\"GET\"])\n@kb.input(\n    \"docs\",\n    provider=\"notion\",\n    query=lambda req: req.params.get(\"q\", \"\"),\n    connection=\"%NOTION_TOKEN%\",\n    include_content=True,\n    content_max_chars=4000,\n    max_depth=4,\n    max_blocks=500,\n)\ndef search(req, docs): ...\n</code></pre>"},{"location":"providers/notion/#behavior","title":"Behavior","text":"<ul> <li><code>search</code> \u2014 queries Notion filtered to <code>page</code> objects, honoring <code>top</code> as   the page size. Each result page is converted to a <code>Document</code>; when   <code>include_content=True</code>, page blocks are fetched and rendered to text.</li> <li><code>get_document</code> \u2014 retrieves a single page by id and renders its block   content, truncated to <code>content_max_chars</code> when that limit is set (via   <code>_render_content</code>). <code>metadata</code> includes the raw <code>blocks</code> and <code>properties</code>.</li> <li>Content extraction \u2014 child blocks are walked depth-first with pagination   and recursion into children, bounded by <code>max_depth</code> / <code>max_blocks</code> and   protected against cycles. Text is emitted in natural reading order.</li> </ul>"},{"location":"providers/notion/#returned-document","title":"Returned <code>Document</code>","text":"Field Notion source <code>document_id</code> Page id <code>title</code> First <code>title</code>-typed property <code>url</code> Page <code>url</code> <code>content</code> Rendered block text (empty when <code>include_content=False</code>) <code>source</code> <code>\"notion\"</code> <code>metadata</code> <code>{\"properties\": ...}</code> for search; also <code>{\"blocks\": ...}</code> for <code>get_document</code>"},{"location":"providers/notion/#errors","title":"Errors","text":"Situation Exception <code>notion-client</code> not installed <code>ProviderError</code> Connection mapping missing <code>token</code>/<code>api_key</code> <code>AuthError</code> Client initialization failure <code>AuthError</code> Notion API error during search/retrieve/block fetch <code>ProviderError</code> Unset <code>%VAR%</code> in the connection string <code>ConfigurationError</code>"},{"location":"providers/notion/#rate-limits","title":"Rate limits","text":"<p>The Notion API is rate-limited. To reduce request volume, lower <code>max_blocks</code> / <code>max_depth</code>, or set <code>include_content=False</code> when you only need titles and URLs.</p>"}]}